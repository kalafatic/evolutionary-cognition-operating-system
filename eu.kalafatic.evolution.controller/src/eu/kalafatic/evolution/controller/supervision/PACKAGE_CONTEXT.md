# SUPERVISION DOMAIN CONTEXT

## Role
Central authority for decision logic and policy enforcement.

## Components
* `DecisionResolver`: Resolves multiple signals into a single action.
* `ActivationResolver`: Determines if a proposal is ready for execution.
* `ResolverPolicy`: Modular logic for ranking and selection.

## Evolutionary Relevance
Critical. The only domain permitted to trigger state transitions.
