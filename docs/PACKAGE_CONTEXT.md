# PACKAGE CONTEXT

## Directory: docs/

## Domain: general

## Components
* `DARWIN_ITERATION.md`: Traditional agents follow a linear path. The AI Evolution Platform uses **Darwinian Evolution** to explore the "Intent Space" through parallel branching. 1. **Mutation**: `DarwinEngine` generates multiple proposal branches (`BranchVariant`) for a given goal. 2. **Exploration**: Each branch is executed in an isolated worktree.
* `PROMPT_SYNTHESIS.md`: Prompt Synthesis transforms the evolved understanding of a target into a high-signal prompt optimized for frontier AI models. 1.  **Intent:** The original user request. 2.  **Architecture Inference:** High-level description of the system structure.
* `TARGET_ANALYSIS_PIPELINE.md`: The mediated analysis pipeline consists of several iterative passes to evolve the kernel's understanding of a target. 1.  **Target Selection:** User selects a directory, file, or mixed workspace. 2.  **Surface Scanning:** Recursive scan to build a file model and detect technologies.
* `SEMANTIC_WORKSPACE_ARCHITECTURE.md`: The Semantic Workspace is a persistent reasoning environment for the orchestration kernel. It solves the problem of "isolated prompt execution" by providing a mechanism for iterations to share semantic knowledge, track long-running trajectories, and avoid redundant rediscovery of conclusions. The system treats the orchestration process as a "reasoning operating system" where conclusions are persisted artifacts rather than transient prompt outputs. 1.  **Semantic Continuity**: Reasoning fragments (conclusions, decisions, patterns) persist across iterations.
* `AI_KERNEL_ARCHITECTURE.md`: --- The platform is not designed as a traditional chatbot. It is evolved into a modular capability platform capable of:
* `example-plan.json`: { "iteration": 12, "variant": "A",
* `PROJECT_SPECIFICATION2.md`: The Evolution Project is an agentic, AI-driven development environment integrated with the Eclipse RCP platform. It utilizes an EMF-based (Eclipse Modeling Framework) domain model to orchestrate autonomous development tasks, leveraging local (Ollama) and remote (OpenAI-compatible, Gemini) LLM providers through a sophisticated hybrid routing system. --- - **EvoProject**: The root container for multiple orchestrations.
* `AGENTS.md`: You are assisting in the development of a deterministic evolutionary kernel. Architecture is STRICT and non-negotiable. For every change you make to this codebase, you MUST: 1.  **Read and follow [ARCHITECTURE_GUIDELINES.MD](ARCHITECTURE_GUIDELINES.MD)**.
* `SCHEDULING_ARCHITECTURE.md`: This document describes the deterministic Backpressure and Execution Scheduling Layer implemented in the AI Kernel. All Darwin execution is treated as **scheduled compute tasks under limited cognitive resources**. The system prioritizes stability and predictability over unbounded parallel exploration. The central execution governor. It decides which Darwin variants are allowed to proceed to evaluation based on the current system state and available budget.
* `ARCHITECTURAL_ANALYSIS.md`: The following entities represent the discovered fundamental building blocks of the cognitive runtime: | Primitive | Semantic Meaning | Lifecycle Role | Ownership | | :--- | :--- | :--- | :--- |
* `IMPROVEMENTS_ANALYSIS.md`: The Evolution platform is built on an EMF-based core with a multi-agent orchestration engine. It successfully integrates into the Eclipse RCP environment and provides several advanced features for AI-assisted development. - **Status**: The platform provides a clear distinction between simple chat and complex orchestration. - **Strength**: The Plan–Execute–Verify (PEV) loop in `EvolutionOrchestrator` ensures traceability and basic quality control.
* `AI_KERNEL_ARCHITECTURE_REFACTOR.md`: Recommended Refactor Order This order minimizes architectural damage while steadily stabilizing the platform. Do NOT jump directly into “smart AI” improvements first.
* `USE_CASE_SIMPLE_TASK.md`: A user wants to create a simple utility class in a Java project. - **User Prompt**: "Create a StringUtils class with a capitalize method in package com.example.util" - **IntentService**: Detects `IMPLEMENTATION` intent.
* `AMBIGUITY_LIFECYCLE.md`: 1.  **Received:** Raw user input is captured. 2.  **Expansion:** `IntentExpansionEngine` decomposes the input into `IntentDimensions`. 3.  **Analysis:** Each dimension is scored for ambiguity.
* `DECISION_AUTHORITY_ARCHITECTURE.md`: The Darwin orchestration system has been refactored to introduce a centralized deterministic decision authority layer called **ActivationResolver**. This layer ensures that all activation decisions (branch selection, winner determination, rank finalization) have a single, reproducible origin. ```text Evaluators (Telemetry)
* `USE_CASE_ITERATIVE_REFACTOR.md`: A user wants to refactor a complex, messy class to follow SOLID principles. - **User Prompt**: "Refactor the OrderProcessor class to improve maintainability and remove the God Object smell." - **IntentService**: Detects `REFACTORING` and `ANALYSIS` intents.
* `PEER_REVIEW_DESIGN.md`: ```text +----------------------------------------------------------------------------------+ |                                     UI Layer                                     |
* `MEDIATED_USE_CASES.md`: A user points the platform to a new repository. Mediated mode analyzes the structure, identifies the core patterns, and generates a prompt for an LLM to perform a deep architectural audit. A user wants to migrate a legacy Java project to a modern Spring Boot architecture. Mediated mode identifies the legacy components (Servlets, manual XML config) and prepares a transition roadmap prompt. A user needs comprehensive documentation for a complex mixed-language workspace. Mediated mode discovers the relationships between modules and synthesizes a prompt to generate high-quality READMEs and API docs.
* `SUPERVISION_AND_AUTHORITY.md`: The platform implements a strict hierarchy to ensure safety and determinism. | Level | Component | Role | Authority | |---|---|---|---|
* `STRATEGY_SMALL_MODELS.md`: Building powerful AI systems—such as coding assistants or autonomous development platforms—does not require massive, multi-billion parameter models if the underlying system architecture is robust. By treating the Large Language Model (LLM) as a specialized execution component rather than a "brain," and by shifting the burden of logic to a structured orchestration layer, developers can achieve complex, high-reliability results using small, resource-constrained local models. Small models struggle with abstract, multi-purpose prompts. The architecture must implement a specialized router to detect the operational mode (e.g., Chat, Coding, Darwin/Iterative, Self-Development) and dispatch the request to a domain-specific agent with a narrowed scope. Before any execution occurs, a specialized "Intent Gate" analyzes the request against a structured requirement schema.
* `CONTEXT_CURATION.md`: Context Curation is the process of selecting a minimal, high-signal set of artifacts to provide to an external LLM. - **Anchors:** Key architectural files like `pom.xml` or `package.json`. - **Entry Points:** Files containing `main` methods or core controllers.
* `CHANGELOG.md`: - **Date**: Current - **Goal**: Implement the architect/self-development loop where JULES treats each prompt/commit as a documented iteration. - **Changes**:
* `MIGRATION_SIGNAL_ARCHITECTURE.md`: This document outlines the refactored signal-based evaluation architecture introduced in the Darwin platform. It serves as a foundation for centralized deterministic decision-making. A standardized telemetry object used by all evaluators to report observations. - `variantId`: Unique identifier for the proposal branch.
* `USE_CASE_MEDIATED_EXECUTION.md`: A user wants to make a high-risk architectural change in `MEDIATED` mode. - **User Prompt**: "Change the event bus from synchronous to asynchronous across the whole platform." - **IntentService**: Detects `ARCHITECTURE`, `REFACTORING`, and `ANALYSIS`.
* `test_all_output.txt`: [INFO] Scanning for projects... [INFO] Tycho Version:  4.0.5 (bc6c4158d7b008797d3e85c0dae0ccd9e50458e5) [INFO] Tycho Mode:     extension
* `PACKAGE_CONTEXT.md`: 
* `pom.xml`: <project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
* `SELF_DEV_ANALYSIS.md`: The self-development functionality allows the Evolution system to autonomously improve its own codebase. It operates in iterative cycles, leveraging either a straightforward task-based refinement or a Darwinian evolutionary approach with parallel variant evaluation. --- 1.  **Session Initiation:** `SelfDevSupervisor` coordinates the session lifecycle (start, stop, iteration loop).
* `ai.txt`: Kernel ├─ Policy Engine ├─ Flow Resolver
* `PROJECT_SPECIFICATION.md`: The Evolution Project is an agentic, AI-driven development environment integrated with the Eclipse RCP platform. It utilizes an EMF-based (Eclipse Modeling Framework) domain model to orchestrate autonomous development tasks, leveraging local (Ollama) and remote (OpenAI-compatible, Gemini) LLM providers through a sophisticated hybrid routing system. --- - **EvoProject**: The root container for multiple orchestrations.
* `USE_CASE_SELF_DEVELOPMENT.md`: The AI kernel identifies a bottleneck in its own `SignalBus` and proposes an optimization. - **System Intent**: "Optimize SignalBus performance by introducing a concurrent lock-free queue." - **IntentService**: Detects `OPTIMIZATION` and `SELF-DEVELOPMENT`.
* `ARCHITECTURE.md`: The AI Evolution Platform is a deterministic, state-transition-based evolutionary kernel designed for autonomous and semi-autonomous software development. It leverages an orchestration-first approach where the kernel manages intelligence rather than being replaced by it. The platform is not a chatbot; it is a **Modular Capability Platform**. It treats AI models as pluggable "coprocessors" while maintaining absolute control over state and execution through a centralized kernel. The system operates under a strict hierarchy:
* `INTENT_EXPANSION_ARCHITECTURE.md`: The Intent Expansion Engine is a critical addition to the Darwin Orchestration Kernel. It addresses the issue of "implementation-first" behavior for ambiguous prompts by introducing a structured exploration of the intent space before any implementation variants are generated. - **Role:** Analyzes the raw user prompt for ambiguity. - **Output:** `IntentExpansionResult` containing dimensions of ambiguity and coherent hypotheses.
* `EVENT_SIGNAL_TRAJECTORY.md`: The kernel is built on a `RuntimeEventBus` that categorizes all system activity into seven categories: - **KERNEL**: System lifecycle (Start, Stop, Mode changes). - **FLOW**: Orchestration control (Iteration start/stop).
* `HYBRID_MODE_DESIGN.md`: Large Language Models (LLMs) running locally (e.g., via Ollama) are excellent for privacy and cost-efficiency. However, models that fit on consumer hardware (7B to 14B parameters) often struggle with: The Evolution Platform implements a **Hybrid Mode** that decouples "Context Gathering" from "Deep Reasoning." 1.  The local model performs a deterministic scan of the project structure.
* `STABILIZATION.md`: Stabilization is about reinforcement, not simplification. The goal is to make the existing complex architecture reliable, traceable, and deterministic. - **Semantic Stabilization**: Ensuring terms like "Variant", "Signal", and "Trajectory" have consistent meanings across the codebase. - **Architectural Clarity**: Explicitly documenting boundaries and authority.
* `EXECUTION_MODES.md`: Platform modes define the environment and infrastructure where the AI operates. - **LOCAL**: Execution occurs entirely on the local machine using local LLMs (e.g., Ollama). - **PROXY**: Requests are routed through a proxy to remote services.
* `GENERAL_MEDIATED_MODE.md`: General Mediated Mode is an evolutionary intelligence mediation layer that deeply analyzes arbitrary targets to prepare high-quality contextual cognition for large-model interaction. - **Mediated AI:** The platform acts as a bridge between the target and the external LLM. - **Evolutionary Context:** Understanding of the target is evolved through multiple analysis passes.
* `CAPABILITY_PLATFORM_ARCHITECTURE.md`: The AI Kernel has evolved into a modular Capability and Plugin Platform. This architecture decouples core orchestration logic from specific implementation details, allowing for safer experimentation and easier extension. The universal extension model. Every major subsystem is now a Capability. - `capabilityId`: Unique identifier.
* `COGNITIVE_TRACING_ARCHITECTURE.md`: The Cognitive Tracing architecture evolves the platform from simple event-emitting orchestration to a causally explainable reasoning infrastructure. It allows the kernel to explain *why* decisions were made, *why* failures occurred, and *why* specific paths were taken. Represents a complete causal chain of reasoning events for a single orchestration lifecycle. It tracks: - Intent expansion paths
* `USE_CASES.md`: - [Simple File Creation](USE_CASE_SIMPLE_TASK.md) - Standard chat interactions. - [Iterative Refactoring](USE_CASE_ITERATIVE_REFACTOR.md)
